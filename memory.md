# memory.md — matchavez/matchavez

Self-context for Claude. This is GitHub's special "profile README" repo (repo name matches username) — its README.md renders on Mat's github.com profile page. Last refreshed: 2026-07-11.

## What this repo is
Just two things: `README.md` (profile blurb) and `reddevils-nzihl-integration.zip` (a packaged deliverable, not source-controlled as loose files — see below).

## Contents
- `README.md` — currently two lines pointing to Dumptruck (Obsidian/notetaking capture tool) and Tellyhoova (video downloader). Kept intentionally short; this is a public profile page, not a project doc.
- `reddevils-nzihl-integration.zip` — a Next.js drop-in package for auto-feeding Fixtures/Standings/Top Scorers from NZIHL onto reddevils.co.nz, added 2026-06-10. This is stored as a **zip artifact**, not unpacked source — if resuming that project, unzip it locally to inspect/edit rather than expecting a browsable source tree here. See Claude's cross-session memory (`reddevils_nzihl_integration.md`) for the scraping conventions that package encodes (scoreboard away-first/home-second, `div.col.mx-1`, printPage URLs, Chrome-capture gotchas).

## Sync note
Keep this file and README.md in sync with any change. If they drift, flag it to Mat and get approval before publishing the sync rather than doing it silently.
